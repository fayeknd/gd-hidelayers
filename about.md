# HideLayers

Adds an in-editor toggle to completely hide objects in other layers, rather than lowering their opacity slightly. 
This can be useful for large, complicated projects when you want to single out one layer. 
