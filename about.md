# HideLayers

A simple mod that adds a toggle which hides objects in the editor that aren't on the layer you're currently on.
Usually, any objects on another layer are visible, but on a low opacity. This just adds a small toggle next to the layer
navigation buttons to hide them completely instead.
