1.0.6 - fixed a crash which the last fix caused whooopspssssss
1.0.5 - fixed a bug where the GameObject's setOpacity function would return early, before the initial function had run, causing some objects to have a full opacity in levels.
